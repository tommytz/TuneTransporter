namespace TuneTransporter;

public class FileTransfer(TrackInfo trackFile, string previousPath)
{
    public readonly TrackInfo TrackFile = trackFile;
    public readonly string PreviousPath = previousPath;
}